WARNING: You *SHOULD NOT* use `clojure.core/read-string` to read data
from untrusted sources.  See the docs for `clojure.core/read`, because
the same security issues exist for both `read` and `read-string`.
